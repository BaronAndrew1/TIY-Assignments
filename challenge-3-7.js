// 100% not working in the least

var space = " ";
var str = "floofloo flam fling"
function letterCapitalize(string, separator) {
  str.split(space);
  str.charAt(0).toUpperCase();
}

console.log(letterCapitalize(str, space));
